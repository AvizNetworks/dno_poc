apiVersion: provisioning.dpu.nvidia.com/v1alpha1
kind: DPUDevice
metadata:
  name: SERVER_NAME-bf3
  namespace: dpf-operator-system
spec:
  # bmcIp is immutable once set — update serial/IP carefully per environment.
  serialNumber: "BF3_SERIAL"
  bmcIp: "BF3_BMC_IP"
